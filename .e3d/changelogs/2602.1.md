## 2026-02-27 - ODR + MCP integration progress

### Completed
- Added reference architecture docs for MAF + MCP + Windows ODR local host model.
- Added architecture diagram spec and generated `docs/architecture.png`.
- Added 2-minute demo script aligned to architecture and current working flow.
- Updated hackathon plan to include:
  - environment update (Windows 11 25H2, ODR v0.1.1)
  - ODR sub-track, evidence requirements, and risk handling.
- Created ODR verification materials:
  - `docs/odr-verification-checklist.md`
  - `docs/odr-runbook.md`
  - `docs/odr-validation-log-2026-02-27.md`
- Implemented `excel-mcp` wrapper over existing `excel-bridge` REST API:
  - `excel-mcp/server.py`
  - `excel-mcp/manifest.json`
  - `excel-mcp/pyproject.toml`
  - `excel-mcp/README.md`

### Validation outcomes
- ODR CLI available and healthy (`odr --version` = `0.1.1`).
- ODR MCP commands functional (`run`, `list`, `add`, `remove`, `configure`).
- Excel MCP manifest registration succeeded.
- Current limitation: ODR CLI access to custom registered server returns `AccessStatus=DeniedBySystem` (policy/client context gate).

### Next actions
- Confirm ODR test-mode/allowlist path for custom server invocation.
- Validate one full ODR-routed run: read PQ -> calculate -> write outputs.
- Keep stable Azure+bridge path as primary demo fallback.

---

## 2026-02-28 - MVP Freeze: Azure Agent & Streamlit Dashboard

### Completed
- Investigated `AccessStatus=DeniedBySystem` for ODR CLI invocation and confirmed it is gated by Windows Secure Boot policy preventing `TESTSIGNING ON` in the Developer Preview.
- Pivoted local execution to the Azure Agent direct proxy MVP path (avoiding ODR runtime block).
- Refactored `azure-agent/agent.py` and extracted orchestration logic into `agent_core.py`.
- Built a modern web-based chat interface (`azure-agent/app.py`) using Streamlit as the primary user dashboard, proving the CalcsLive "System Utility" concept.
- Successfully executed end-to-end multi-agent orchestration via the browser:
  - Azure Agent <-> Local Excel Bridge API <-> Excel COM <-> CalcsLive API
- Updated MVP dependencies (`requirements.txt`) to include `streamlit`.

### Next actions
- Freeze MVP scope.
- Update documentation (`README.md` and architecture diagram) to reflect the Streamlit Dashboard topology and ODR Secure Boot limitations.
- Record the 2-minute demo video showing the unit-aware calculation flow in Excel through the Streamlit interface.
